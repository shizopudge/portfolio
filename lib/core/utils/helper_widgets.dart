import 'package:flutter/cupertino.dart';

Widget addVerticalPadding(double padding) => SizedBox(
      height: padding,
    );

Widget addHorizontalPadding(double padding) => SizedBox(
      width: padding,
    );
